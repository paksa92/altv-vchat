const config = {
    content: ['./src/**/*.{ts,tsx}'],
    theme: {
        fontFamily: {
            sans: ['Inter', 'sans-serif'],
            serif: ['Syne', 'serif'],
        },
    },
    plugins: [],
};

module.exports = config;
